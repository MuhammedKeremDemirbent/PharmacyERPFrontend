Yapılanlar baştan sona burada depolanacak 

node.js indir
node -v
npm -v 
npm create vite@latest
cd erp_frontend
npm install
npm run dev

29.01.2026 tarihinde backend ile bağlantı kuruldu (viteconfig.ts) database bağlantısıda yapılmış oldu backend çalışmazsa frontend çalışır ancak veri gelmez
çoğu sayfa hazır barkod okuyucu entegrasyonu kaldı görsele gerek yok kaldırılcak account sayfası olcak şifre alma şifre değiştirme eklenecek smtp ile olur 
backend kısmı için biten ilaçlar için uyarı sistemi eklendi celery ile alıyoruz bunu maile dökmeyi deneyelim.

31.01.2026 tarihinde kullanıcı ekleme sayfasına şifre koruması eklendi.
Sırada görselleri kaldırdık backend kısmında biten ilaçlar için uyarı sistemi eklendi celery ile alıyoruz bunu maile dökmeyi deneyelim.
.env eklendi şifreler vs oraya yazıldı. 

Devamında frontend kısmında müşteri girme yeri eklendi ve satış yapılırken müşteri seçimi eklendi
Müşteri kaydetme ve eczacı ile müşteriyi databaseye ekledik
00.00 da smtp ile mail gönderme sistemi eklendi günlük ciro yazıyor

01.02.2026 tarihinde birazcık kodları incele eksiklerini düzelt

Tedarikçiye mail atma müşteriye p.tesileri mail atma e fiş atma 

02.02.2026 SADECE DATABASE(SALES) KISMI UTC AYARI DÜZELTİLDİ. DATABASE UTC BİZ TR SAATİNE GÖRE GÖRÜYORUZ.


redux toolkit ekle 
redux tooklit axios kullanılcak belki. 
listenin en başlarına auth store ekle İlaç store olcak, patient store, 

auth store ( auth token , refresh token için ) RTK + redux persist (sadece authta kullan)
user store: ( user profile bilgileri, user diğer aggregated ya da required relationship için )
ilaç store: api kullanımı için rtk
patiente store: api kullanımı için rtk
...
auth refresh kur rtk için 1 dk kala req atma refresh atsın tokene.  await mutex.waitForUnlock(); 


Görüşmeden Sonra Yapılanlar

npm install @reduxjs/toolkit react-redux indirildi redux toolkit için amaç burada sayfa değişince
bile sepet bilgileri korunması

npm install redux-persist auth için

store klasörüne sepet ve user için redux eklendi

npm install jwt-decode async-mutex

atom molekül yapısı inşa edildi

shadcn için npm install -D @types/node indirdik
npm install class-variance-authority clsx tailwind-merge lucide-react
npx shadcn@latest add button input label table select card dialog alert textarea checkbox popover
npm install class-variance-authority clsx tailwind-merge lucide-react tailwindcss-animate
npx shadcn@latest add button input label table select card dialog alert textarea checkbox popover -y
npm install -D @types/node
npm install @radix-ui/react-slot




npm install -D @types/node
npx shadcn@latest init
npx shadcn@latest add button input card dialog label select table alert textarea checkbox popover -y
npm install class-variance-authority clsx tailwind-merge lucide-react tailwindcss-animate @radix-ui/react-s
npx shadcn@latest add badge -y
npx shadcn@latest add table dialog -y



SIMPLE_JWT_SIGNING_KEY = os.getenv("SIMPLE_JWT_SIGNING_KEY", os.getenv("SECRET_KEY", "django-insecure-please-change-me"))

# simple_jwt settings
ACCESS_TOKEN_TIMEOUT = int(os.getenv("ACCESS_TOKEN_TIMEOUT", 60 * 60))
REFRESH_TOKEN_TIMEOUT = int(os.getenv("REFRESH_TOKEN_TIMEOUT", 60 * 60 * 24))
SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(seconds=ACCESS_TOKEN_TIMEOUT),
    'REFRESH_TOKEN_LIFETIME': timedelta(seconds=REFRESH_TOKEN_TIMEOUT),
    'ROTATE_REFRESH_TOKENS': True,
    'BLACKLIST_AFTER_ROTATION': True,
    'UPDATE_LAST_LOGIN': False,

    'ALGORITHM': 'HS256',
    'SIGNING_KEY': SIMPLE_JWT_SIGNING_KEY,
    'VERIFYING_KEY': None,
    'AUDIENCE': None,
    'ISSUER': None,
    'JWK_URL': None,
    'LEEWAY': 0,

    'AUTH_HEADER_TYPES': ('Bearer',),
    'AUTH_HEADER_NAME': 'HTTP_AUTHORIZATION',
    'USER_ID_FIELD': 'id',
    'USER_ID_CLAIM': 'user_id',
    'USER_AUTHENTICATION_RULE': 'rest_framework_simplejwt.authentication.default_user_authentication_rule',

    'AUTH_TOKEN_CLASSES': ('rest_framework_simplejwt.tokens.AccessToken',),
    'TOKEN_TYPE_CLAIM': 'token_type',
    'TOKEN_USER_CLASS': 'rest_framework_simplejwt.models.TokenUser',

    'JTI_CLAIM': 'jti',
}


İDEMPOTENCY İÇİN npm install uuid @types/uuid indirdik
MAİN.TSX İÇİN İDEMPOTENCY KODUNU EKLEDİK
import { v4 as uuidv4 } from 'uuid'; BENZERSİZ ANAHTAR OLUŞTURMA



store ve services kısımları tamamen değişti rtk query ile entegre edildi


StrictMode canlıya alınınca kapanabilirmiş
keepUnusedDataFor: 5


 Eğer yeni bir API ekleyeceksen, önce types/service.ts içinde tipini tanımla, sonra store/api/ içinde yeni bir dosya oluştur ve en son store/store.ts içinde onu tanıt. En son sayfada kullan!

 cloudflaretunnel